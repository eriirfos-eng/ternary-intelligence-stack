// Module:  stdlib/causal/thermal_filter.test.tern
// Purpose: Unit test suite for thermal_filter
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn thermal_filter_test_verify() -> trit {
    let state: trit = tend;
    if state == tend {
        return affirm;
    }
    return reject;
}
