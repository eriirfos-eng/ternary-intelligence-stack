// Module:  stdlib/bio/thermal_tensor.example.tern
// Purpose: Production usage example for thermal_tensor
// Author:  RFI-IRFOS

fn example_thermaltensor() -> trit {
    let signal: float = 0.75;
    let baseline: float = 0.5;
    
    // Demonstrate usage of the core module
    if signal > baseline {
        return affirm;
    }
    return tend;
}
