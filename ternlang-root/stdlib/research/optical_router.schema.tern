// Module:  stdlib/research/optical_router.schema.tern
// Purpose: T-SON serialization schema for optical_router
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Optical_routerSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Optical_routerSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
