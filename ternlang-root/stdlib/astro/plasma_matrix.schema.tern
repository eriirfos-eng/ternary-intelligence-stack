// Module:  stdlib/astro/plasma_matrix.schema.tern
// Purpose: T-SON serialization schema for plasma_matrix
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Plasma_matrixSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Plasma_matrixSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
