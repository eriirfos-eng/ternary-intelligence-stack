// Module:  stdlib/tests/magnetic_router.example.tern
// Purpose: Production usage example for magnetic_router
// Author:  RFI-IRFOS

fn example_magneticrouter() -> trit {
    let signal: float = 0.75;
    let baseline: float = 0.5;
    
    // Demonstrate usage of the core module
    if signal > baseline {
        return affirm;
    }
    return tend;
}
