// Module:  stdlib/tests/magnetic_engine.schema.tern
// Purpose: T-SON serialization schema for magnetic_engine
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Magnetic_engineSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Magnetic_engineSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
