// Module:  stdlib/tests/optical_model.tern
// Purpose: Core implementation for optical_model
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn execute_optical_model(signal: float, baseline: float) -> trit {
    // Exhaustive ternary matches with explicit tend semantics
    if signal > baseline + 0.5 {
        return affirm; // Super-baseline
    }
    if signal < baseline - 0.5 {
        return reject; // Sub-baseline
    }
    return tend; // Equilibrium
}

fn reduce_optical_model(t: trit) -> int {
    match t {
        affirm => return 1;
        reject => return -1;
        tend => return 0;
    }
}
