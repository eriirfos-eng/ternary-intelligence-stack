// Module:  stdlib/tests/plasma_optimizer.schema.tern
// Purpose: T-SON serialization schema for plasma_optimizer
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Plasma_optimizerSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Plasma_optimizerSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
