// Module:  stdlib/tests/plasma_tensor.example.tern
// Purpose: Production usage example for plasma_tensor
// Author:  RFI-IRFOS

fn example_plasmatensor() -> trit {
    let signal: float = 0.75;
    let baseline: float = 0.5;
    
    // Demonstrate usage of the core module
    if signal > baseline {
        return affirm;
    }
    return tend;
}
