// Module:  stdlib/math/magnetic_tensor.test.tern
// Purpose: Unit test suite for magnetic_tensor
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn magnetic_tensor_test_verify() -> trit {
    let state: trit = tend;
    if state == tend {
        return affirm;
    }
    return reject;
}
