// Module:  stdlib/logic/optical_model.test.tern
// Purpose: Unit test suite for optical_model
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn optical_model_test_verify() -> trit {
    let state: trit = tend;
    if state == tend {
        return affirm;
    }
    return reject;
}
