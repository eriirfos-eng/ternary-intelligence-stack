// Module:  stdlib/logic/optical_system.test.tern
// Purpose: Unit test suite for optical_system
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn optical_system_test_verify() -> trit {
    let state: trit = tend;
    if state == tend {
        return affirm;
    }
    return reject;
}
