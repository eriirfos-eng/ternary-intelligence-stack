// Module:  stdlib/logic/fluidic_tensor_sparse.tern
// Purpose: Sparse matrix execution for fluidic_tensor
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn execute_fluidic_tensor_sparse(signal: float, baseline: float) -> trit {
    // Exhaustive ternary matches with explicit tend semantics
    if signal > baseline + 0.5 {
        return affirm; // Super-baseline
    }
    if signal < baseline - 0.5 {
        return reject; // Sub-baseline
    }
    return tend; // Equilibrium
}

fn reduce_fluidic_tensor_sparse(t: trit) -> int {
    match t {
        affirm => return 1;
        reject => return -1;
        tend => return 0;
    }
}
