// Module:  stdlib/logic/thermal_model.schema.tern
// Purpose: T-SON serialization schema for thermal_model
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Thermal_modelSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Thermal_modelSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
