// Pattern of handling uncertainty with match
fn process(val: int) -> int {
    match (val) {
        1 => return 1,
        -1 => return -1,
        0 => return 0,
        _ => return 0
    }
}
print(process(0));
