// Module:  stdlib/science/thermal_system.schema.tern
// Purpose: T-SON serialization schema for thermal_system
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Thermal_systemSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Thermal_systemSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
