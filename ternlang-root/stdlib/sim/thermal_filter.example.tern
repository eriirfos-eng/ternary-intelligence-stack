// Module:  stdlib/sim/thermal_filter.example.tern
// Purpose: Production usage example for thermal_filter
// Author:  RFI-IRFOS

fn example_thermalfilter() -> trit {
    let signal: float = 0.75;
    let baseline: float = 0.5;
    
    // Demonstrate usage of the core module
    if signal > baseline {
        return affirm;
    }
    return tend;
}
