// Module:  stdlib/security/plasma_solver.schema.tern
// Purpose: T-SON serialization schema for plasma_solver
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Plasma_solverSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Plasma_solverSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
