// Module:  stdlib/security/kinetic_solver.schema.tern
// Purpose: T-SON serialization schema for kinetic_solver
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Kinetic_solverSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Kinetic_solverSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
