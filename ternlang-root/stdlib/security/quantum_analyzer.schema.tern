// Module:  stdlib/security/quantum_analyzer.schema.tern
// Purpose: T-SON serialization schema for quantum_analyzer
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Quantum_analyzerSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Quantum_analyzerSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
