// Module:  stdlib/specs/optical_matrix.schema.tern
// Purpose: T-SON serialization schema for optical_matrix
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Optical_matrixSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Optical_matrixSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
