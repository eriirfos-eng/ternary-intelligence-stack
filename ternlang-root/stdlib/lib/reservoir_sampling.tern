// reservoir_sampling.tern
// Verification of reservoir sampling

fn main() -> trit {
    let reservoir: int[1] = 0;
    let item: int = 42;
    
    // Simplest case: first item
    reservoir[0] = item;
    
    if (reservoir[0] == 42) {
        return affirm;
    }
    return reject;
}
