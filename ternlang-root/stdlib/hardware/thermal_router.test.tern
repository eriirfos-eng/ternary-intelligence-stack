// Module:  stdlib/hardware/thermal_router.test.tern
// Purpose: Unit test suite for thermal_router
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn thermal_router_test_verify() -> trit {
    let state: trit = tend;
    if state == tend {
        return affirm;
    }
    return reject;
}
