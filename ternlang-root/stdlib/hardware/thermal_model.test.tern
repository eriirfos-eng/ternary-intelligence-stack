// Module:  stdlib/hardware/thermal_model.test.tern
// Purpose: Unit test suite for thermal_model
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn thermal_model_test_verify() -> trit {
    let state: trit = tend;
    if state == tend {
        return affirm;
    }
    return reject;
}
