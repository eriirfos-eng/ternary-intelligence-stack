// Module:  stdlib/network/magnetic_solver.schema.tern
// Purpose: T-SON serialization schema for magnetic_solver
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Magnetic_solverSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Magnetic_solverSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
