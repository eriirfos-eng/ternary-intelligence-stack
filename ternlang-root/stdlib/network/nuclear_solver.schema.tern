// Module:  stdlib/network/nuclear_solver.schema.tern
// Purpose: T-SON serialization schema for nuclear_solver
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Nuclear_solverSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Nuclear_solverSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
