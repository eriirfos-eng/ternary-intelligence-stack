fn main() -> trit {
    let arr: int[2] = 0;
    arr[0] = 2;
    arr[1] = 3;
    let i: int = 0;
    while i < 2 {
        arr[i] = arr[i] * arr[i] * arr[i];
        i = i + 1;
    }
    if arr[0] == 8 && arr[1] == 27 { return 1; }
    return -1;
}
