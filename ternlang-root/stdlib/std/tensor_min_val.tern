fn main() -> trit {
    let arr: int[2] = 0;
    arr[0] = 10;
    arr[1] = 20;
    let res: int = 0;
    if arr[0] < arr[1] { res = arr[0]; } else { res = arr[1]; }
    if res == 10 { return 1; }
    return -1;
}
