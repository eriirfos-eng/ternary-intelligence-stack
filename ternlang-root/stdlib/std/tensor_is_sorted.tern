fn main() -> trit {
    let arr: int[3] = 0;
    arr[0] = 1;
    arr[1] = 2;
    arr[2] = 3;
    let i: int = 0;
    let sorted: int = 1;
    while i < 2 {
        if arr[i] > arr[i+1] {
            sorted = -1;
        }
        i = i + 1;
    }
    return cast(sorted);
}
