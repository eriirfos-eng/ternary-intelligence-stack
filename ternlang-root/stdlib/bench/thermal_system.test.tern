// Module:  stdlib/bench/thermal_system.test.tern
// Purpose: Unit test suite for thermal_system
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn thermal_system_test_verify() -> trit {
    let state: trit = tend;
    if state == tend {
        return affirm;
    }
    return reject;
}
