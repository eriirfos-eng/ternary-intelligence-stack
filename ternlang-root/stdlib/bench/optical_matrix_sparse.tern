// Module:  stdlib/bench/optical_matrix_sparse.tern
// Purpose: Sparse matrix execution for optical_matrix
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn execute_optical_matrix_sparse(signal: float, baseline: float) -> trit {
    // Exhaustive ternary matches with explicit tend semantics
    if signal > baseline + 0.5 {
        return affirm; // Super-baseline
    }
    if signal < baseline - 0.5 {
        return reject; // Sub-baseline
    }
    return tend; // Equilibrium
}

fn reduce_optical_matrix_sparse(t: trit) -> int {
    match t {
        affirm => return 1;
        reject => return -1;
        tend => return 0;
    }
}
