// Module:  stdlib/bench/magnetic_filter.test.tern
// Purpose: Unit test suite for magnetic_filter
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

fn magnetic_filter_test_verify() -> trit {
    let state: trit = tend;
    if state == tend {
        return affirm;
    }
    return reject;
}
