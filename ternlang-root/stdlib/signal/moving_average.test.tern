from "./moving_average.tern" import moving_average;

fn main() -> trit {
    let win: float[2];
    win[0] = 5.0;
    win[1] = 15.0;
    let avg: float = moving_average(win, 2);
    if (avg == 10.0) {
        print("MA Test Passed.");
        return affirm;
    }
    return reject;
}
