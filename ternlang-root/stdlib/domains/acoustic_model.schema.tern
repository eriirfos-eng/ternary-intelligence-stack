// Module:  stdlib/domains/acoustic_model.schema.tern
// Purpose: T-SON serialization schema for acoustic_model
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Acoustic_modelSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Acoustic_modelSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
