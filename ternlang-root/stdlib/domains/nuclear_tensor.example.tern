// Module:  stdlib/domains/nuclear_tensor.example.tern
// Purpose: Production usage example for nuclear_tensor
// Author:  RFI-IRFOS

fn example_nucleartensor() -> trit {
    let signal: float = 0.75;
    let baseline: float = 0.5;
    
    // Demonstrate usage of the core module
    if signal > baseline {
        return affirm;
    }
    return tend;
}
