// Module:  stdlib/pipelines/quantum_filter.schema.tern
// Purpose: T-SON serialization schema for quantum_filter
// Author:  RFI-IRFOS
// Ref:     https://ternlang.com

struct Quantum_filterSchema {
    id: int,
    confidence: trit,
    weight: float
}

fn validate_schema(data: Quantum_filterSchema) -> trit {
    if data.weight > 0.0 { return affirm; }
    if data.weight < 0.0 { return reject; }
    return tend;
}
