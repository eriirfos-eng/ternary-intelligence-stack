from setuptools import setup, find_packages

setup(
    name="torch-ternary",
    version="0.1.0",
    packages=find_packages(),
    description="PyTorch backend for the Ternary Intelligence Stack (BET VM).",
)
