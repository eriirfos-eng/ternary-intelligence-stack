[package]
name = "ternlang-tson"
version.workspace = true
edition.workspace = true
license = "LicenseRef-BSL-1.1"
description = "TSON (Ternary JSON) parser. Native triadic state encoding with 30% higher density and proprietary MoE-13 compression."

[dependencies]
ternlang-core = { path = "../compiler/legacy_shim/ternlang-core", version = "0.3.1" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
