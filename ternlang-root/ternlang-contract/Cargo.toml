[package]
name = "ternlang-contract"
version.workspace = true
edition.workspace = true
license = "LicenseRef-BSL-1.1"
description = "Sovereign Smart Contracts for the T-Fi Economy. Self-pausing triadic execution layer bypassing the EVM."

[dependencies]
ternlang-core = { path = "../compiler/legacy_shim/ternlang-core", version = "1.0.0" }
ternlang-consensus = { path = "../ternlang-consensus", version = "0.3.1" }
