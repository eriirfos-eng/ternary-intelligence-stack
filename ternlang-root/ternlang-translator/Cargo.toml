[package]
name = "ternlang-translator"
version.workspace = true
description = "Natural language to ternlang source translator. Converts human-readable specifications to .tern files."
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
clap = { version = "4.4", features = ["derive"] }
regex = "1.10"
colored = "2.1"
anyhow = "1.0"