[package]
name = "ternlang-astro"
version.workspace = true
edition.workspace = true
license = "LicenseRef-BSL-1.1"
description = "Orbital and Satellite Routing Control. MoE-13 orchestrator and T-POSIX framework for non-binary, noisy space communication (GEO-LEO)."

[dependencies]
ternlang-core = { path = "../compiler/legacy_shim/ternlang-core", version = "0.1.0" }
ternlang-posix = { path = "../ternlang-posix", version = "0.1.0" }
ternlang-moe = { path = "../ternlang-moe", version = "0.1.2" }
