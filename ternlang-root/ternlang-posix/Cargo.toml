[package]
name = "ternlang-posix"
version.workspace = true
edition.workspace = true
license = "LicenseRef-BSL-1.1"
description = "T-POSIX integration layer. Injects the triadic state directly into OS kernels for edge, drone, and RTOS environments."

[dependencies]
ternlang-core = { path = "../ternlang-core", version = "0.1.0" }
libc = "0.2"
