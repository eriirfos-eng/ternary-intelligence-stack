[package]
name = "ternlang-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
ternlang-core = { path = "../ternlang-core" }
ternlang-ml   = { path = "../ternlang-ml" }
clap = { version = "4.4", features = ["derive"] }
